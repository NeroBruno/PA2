using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// This will have procedural animations through a spring system to simulate headbobs, shakes(explosions), jump force, fall impact and getting hit to apply to the camera
public class FirstPersonCamera : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
